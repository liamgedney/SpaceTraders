public class Universe {

    public static void createSolarSystems {
        List<String> SolarSystemArr = new ArrayList<>();
        for (int i = 0; i < 10; i++) {
            SolarSystem temp = new SolarSystem("Planet" + i, );
            SolarSystemArr[i] = temp.toString();
        }
    }
}